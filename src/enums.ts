export enum BattleNetNamespace {
  STATIC = 'static',
  DYNAMIC = 'dynamic',
  PROFILE = 'profile'
}

export enum BattleNetRegion {
  EU = 'EU',
  US = 'US'
}

export enum BattleNetScope {
  WOW_PROFILE = 'wow.profile',
  OPENID = 'openid'
}